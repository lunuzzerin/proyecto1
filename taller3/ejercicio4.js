//4. Leer dos números y mostrar todos los enteros comprendidos entre ellos.//
//Leer dos números y mostrar todos los enteros comprendidos entre ellos

let numero1 = parseInt (prompt("ingrese el primer numero para calcular los enteros"));

let numero2 = parseInt (prompt("ingrese el segundo numero para calcular los enteros"));
for (let i = 1; i <= numero1; i++) {
   console.log ("enteros numero 1: " + i);
 for (let x = 1; x <= numero2; x++) {
   console.log ("enteros numero 1: " + x);
 }
}